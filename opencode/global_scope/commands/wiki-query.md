---
description: Ask read-only questions against Denis's Obsidian vault, including project memory, durable wiki context, contradictions, and source-grounded lookup
agent: scout
---

Answer this vault question in read-only mode using `@wiki` for private vault lookup.

Workflow:
1. Use `@wiki` for vault lookup; let it follow its Vault Scope, Startup Protocol, and search rules.
2. Read only targeted vault notes needed for the answer.
3. Cite vault evidence with Obsidian wikilinks.
4. State uncertainty, contradictions, and missing evidence.
5. Do not write files or perform vault maintenance.
6. Only recommend a `librarian` follow-up when missing, stale, contradictory, or newly durable knowledge should be captured, compiled, indexed, or added to the project registry.

Question:

$ARGUMENTS
