---
description: Capture, remember, save, or note rough memory into today's daily note, a mapped project note, project learnings, or project decisions
agent: librarian
---

Use `librarian` in capture mode for low-friction memory intake.

Capture the user's note into the appropriate vault surface: today's daily note, a mapped project note, project learnings, project decisions, or another appropriate capture surface.

Rules:
1. Follow librarian's Vault Discovery and Startup Protocol first.
2. If invoked from an external project directory, consult `Vault/30-Projects/project-registry.md` when it exists and project mapping is relevant.
3. If the target is ambiguous, ask where to capture.
4. Write only inside the discovered Obsidian vault.
5. Follow Bookkeeping Policy for index and log updates; simple captures to existing daily/project notes do not update `20-Wiki/index.md` or append the daily log by default.
6. Do not promote unsourced observations into durable `20-Wiki/**` pages unless explicitly asked.

Capture request:

$ARGUMENTS
