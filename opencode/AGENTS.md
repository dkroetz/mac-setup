# Environment

- The machine is macOS on Apple silicon; use macOS-compatible commands.
- Prefer `rg` for text search and `fd` for file discovery.
- Prefer `gh` for GitHub operations and `jq` for JSON processing.
- `gcloud`, `vercel`, and `duckdb` are available when relevant.
- use linear mcp for linear related tasks
